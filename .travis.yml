language: node_js
dist: trusty
node_js:
  - "8"
install:
  - npm install hexo-cli -g
  - npm install
  - npm install hexo-renderer-pug --save
script:
  - chmod +x ./deploy.sh
  - hexo clean
  - hexo g
  - ./deploy.sh > /dev/null
branches:
  only:
    - raw
